{
  const username = document.querySelector(".username");
  username.style.color = COLORS[226]; // '#ffff00';

  const separator = document.querySelector(".separator");
  separator.style.color = COLORS[220]; // '#ffd700';

  const hostname = document.querySelector(".hostname");
  hostname.style.color = COLORS[214]; // '#ffaf00';

  const relativeDir = document.querySelector(".relative-dir");
  relativeDir.style.color = COLORS[33]; // '#005fff';

  const cmdSeparator = document.querySelector(".cmd-separator");
  cmdSeparator.style.color = '#fff';
}
